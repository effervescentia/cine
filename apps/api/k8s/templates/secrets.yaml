apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: "{{ include "cine-api.fullname" . }}-secrets"
  labels:
    {{- include "cine-api.labels" . | nindent 4 }}
stringData:
  POSTGRES_PASSWORD: "ref+sops://k8s/secrets.yaml#/POSTGRES_PASSWORD"

  REDIS_PASSWORD: "ref+sops://k8s/secrets.yaml#/REDIS_PASSWORD"
