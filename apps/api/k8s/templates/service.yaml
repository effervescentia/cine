apiVersion: v1
kind: Service
metadata:
  name: {{ include "cine-api.fullname" . }}
  labels:
    {{- include "cine-api.labels" . | nindent 4 }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/component: server
    {{- include "cine-api.selectorLabels" . | nindent 4 }}
