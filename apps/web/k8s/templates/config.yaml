apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cine-web.fullname" . }}
  labels:
    {{- include "cine-web.labels" . | nindent 4 }}
data:
  PORT: {{ .Values.service.port | quote }}
  PORT_HEALTH: {{ .Values.health.port | quote }}

  CINE_API_HOST: {{ .Values.api.host }}
