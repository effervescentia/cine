apiVersion: v1
kind: Secret
metadata:
  name: "\{{ include "{{kebabCase name}}-db.fullname" . }}-migration-secrets"
  labels:
    \{{- include "{{kebabCase name}}-db.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_PASSWORD: "ref+sops://k8s/secrets.yaml#/POSTGRES_ADMIN_PASSWORD"
